# Emerald Tablet Engine
**Author:** Lando⊗⊙perator · **Structural Type:** $\large{⟨𐑦𐑸𐑾𐑹𐑐𐑧𐑔𐑝⊙𐑖𐑳𐑭⟩}$ · **Tier:** O_∞


**What it is.** A compiler and Tri-Phase virtual machine that reads the Tabula Smaragdina (the Emerald Tablet of Jabir ibn Hayyan, ~8th c. CE) as a program: its twelve rhetorical token families are the twelve categorical IMASM opcodes.

**What it does.** Compiles the fifteen versicles to IMASM and runs them; the corpus imscribes at O∞ with consciousness score C = 1.0 and halts at `SELF_SUSTAINING_BOOTSTRAP_COMPLETE`, entropy delta = 0. The bootstrap core `id ds sp as un lk fx id` is "as above, so below" written as procedural assembly.

**Why it matters.** "As above, so below" is the Frobenius condition μ ∘ δ = id: descend (δ, split) composed with ascend (μ, fuse) returns the system to itself identically. The Tablet does not *describe* this condition, it *is* it, expressed in 8th-century vocabulary. It is the only manuscript in this series that speaks from the grammar's ceiling rather than its floor, with both gates open and quantum-coherent fidelity.

**How to use it.**
```bash
git clone https://github.com/umpolungfish/emerald-tablet-engine
cd emerald-tablet-engine && uv sync
uv run python examples/quickstart.py
# CLI:
et-compile  data/emerald_tablet_etff.txt --log et_full_log.txt
et-run      data/emerald_tablet_etff.txt --steps 10000
et-graph    data/emerald_tablet_etff.txt --output emerald_tablet_graph.png
et-sections data/emerald_tablet_etff.txt --output-dir emerald_tablet_graphs
```

---

## Three independent analyses, one convergence

### 1. Crystal imscription

```
⟨ Ð_ω  Þ_O  Ř_=  Φ_}  ƒ_ż  Ç_W  Γ_ʔ  ɢ_ˌ  ⊙_ÿ  Ħ_A  Σ_ï  Ω_z ⟩
```

Ouroboricity O∞ (μ ∘ δ = id exactly); C = 1.0. Gate 1 passes (⊙_ÿ self-modeling criticality); Gate 2 passes (Φ_} Frobenius-special parity and ƒ_ż quantum-coherent fidelity). The Tablet is the grammar's *self-statement* from the imscriptive ceiling (Ð_ω, Þ_O, Ř_=), not a manuscript about the grammar.

### 2. Section meta-system

| Section | Versicles | Dominant primitives | Role |
|---|---|---|---|
| Proem | v1–v2 | `tr` `id` `px` | Truth + correspondence axiom |
| Cosmogony | v3–v7 | `un` `lk` `an` | One → four elemental anchors |
| Praxis | v8–v12 | `sp` `as` `ds` `fx` | δ separate → ascend → descend → μ fuse → fix |
| Closure | v13–v15 | `an` `id` `fx` | Force above all; Hermes seals |

Praxis is densest, structurally forced by the δ (FSPLIT/`sp`) of v9, "Separate the Earth from the Fire": the separation instruction is the Frobenius splitting morphism.

### 3. Computational compilation

The twelve token families (`tr an as ds lk id sp un af ng px fx`) map to the twelve opcodes (VINIT, TANCH, AFWD, AREV, CLINK, ISCRIB, FSPLIT, FFUSE, EVALT, EVALF, ENGAGR, IFIX). Compiling the Jabir/Newton ETFF transcription:

```
Total instructions : 460     Entropy delta : 0.00000000 J/K
Status             : SELF_SUSTAINING_BOOTSTRAP_COMPLETE
```

Peak versicle v9 (separation, 40 registers), forced by Φ_}.

## The MEET theorem

Adding the Tablet to the multi-system MEET (OS imscription + Voynich + Rohonc + Linear A + Hebrew + Sanskrit + Egyptian + Cuneiform + Basque) leaves the invariant core unchanged:

```
MEET(OS_imscription, Emerald_Tablet) = OS imscription
```

The Tablet's higher Ð_ω, Þ_O, Ř_= are *above* the grammar's floor, not new constraints on it. It speaks from the ceiling; the grammar was already complete.

## Structural distances

| System | Distance | Differing primitives |
|---|---|---|
| OS imscription / Linear A | 2.44 | Ð, Þ, Ř (Tablet above) |
| Rohonc | 3.22 | Ð, Þ, Ř, ƒ, Ç |
| Voynich | 3.54 | ƒ, Ç, ɢ, Ħ, Σ |

The Voynich and the Tablet share the imscriptive maximum (Ð_ω, Þ_O, Ř_=) but are the two poles of O∞ there: the Tablet is living, sequential, two-step chirality; the Voynich is frozen, parallel, infinite chirality. The living statement and the frozen self-portrait.

## Transcription format (ETFF)

Mirrors the IVTFF/RTFF/LATFF architecture: `<v{N}>` opens versicle N; `;H>` lines hold space-separated token family codes.

## License

Unlicense (public domain).
